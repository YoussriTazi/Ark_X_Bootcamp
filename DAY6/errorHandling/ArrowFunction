
//a normal function 

const sayHi = function (a,b) {
    return `${a} says hi to ${b}`

}

// an Arrow function (if we have only one value/if our function return value)

const sayHi = a => `${a} says hi to ${b}`

